/** Passthrough helpers — keep call sites simple; no special styling. */
export const highlightText = (text) => text || "";
export const highlightMobileText = (text) => text || "";
export const highlightHeadlineText = (text) => text || "";
export const highlightProblemSectionText = (text) => text || "";
